rules_version = '2';

// No client-side Firebase Storage usage in this project. Deny all access.
// (Any privileged writes would go through the Admin SDK, which bypasses rules.)

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
