name: Deploy to Cloud Run

# Deploys the MCP server container to Cloud Run when CI passes on main.
# Prereqs: see docs/cloud-setup.md for one-time GCP/GitHub setup.

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]
  workflow_dispatch:  # allow manual trigger for redeploys

permissions:
  contents: read
  id-token: write  # required for Workload Identity Federation

env:
  SERVICE_NAME: skill-library-mcp

jobs:
  deploy:
    name: Build & deploy
    runs-on: ubuntu-latest

    # Only run on CI success (or manual trigger); never on CI failure.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}

    concurrency:
      group: deploy-cloud-run
      cancel-in-progress: false

    steps:
      # Pin to the exact commit that CI validated. On workflow_run,
      # github.sha is the default branch HEAD at workflow-start time,
      # which can race with a subsequent push. head_sha is the commit
      # that actually triggered CI.
      - name: Checkout CI-validated commit
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Compute deploy SHA + image URI
        id: image
        run: |
          SHA="${{ github.event.workflow_run.head_sha || github.sha }}"
          URI="${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.AR_REPO }}/${{ env.SERVICE_NAME }}:${SHA}"
          echo "sha=${SHA}" >> "$GITHUB_OUTPUT"
          echo "uri=${URI}" >> "$GITHUB_OUTPUT"

      - name: Authenticate to GCP (Workload Identity Federation)
        id: auth
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.DEPLOY_SA_EMAIL }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v3

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet

      # Build in the GH runner (not Cloud Build). Avoids the post-2024
      # Cloud Build service-account setup dance on new projects.
      - name: Build image
        run: docker build -t "${{ steps.image.outputs.uri }}" .

      - name: Push image
        run: docker push "${{ steps.image.outputs.uri }}"

      # --service-account pins the least-privilege runtime SA (Secret Manager
      # accessor on the three secrets + roles/datastore.user for telemetry).
      # Without it Cloud Run falls back to the default Compute SA with broad
      # Editor — an unacceptable identity for a public unauthenticated service.
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy "${{ env.SERVICE_NAME }}" \
            --image="${{ steps.image.outputs.uri }}" \
            --region="${{ vars.GCP_REGION }}" \
            --project="${{ vars.GCP_PROJECT_ID }}" \
            --platform=managed \
            --allow-unauthenticated \
            --service-account="${{ vars.RUNTIME_SA_EMAIL }}" \
            --max-instances=1 \
            --concurrency=8 \
            --memory=2Gi \
            --cpu=1 \
            --timeout=300 \
            --port=8080 \
            --set-env-vars="MCP_REMOTE=true,MCP_READ_ONLY=1,TELEMETRY_FIRESTORE=1,MAINTENANCE_AUTONOMY=conservative,GITHUB_VAULT_REPO=npbuilds/skill-library-vault,HF_HUB_OFFLINE=1,TRANSFORMERS_OFFLINE=1" \
            --set-secrets="GITHUB_BOT_PAT=github-bot-pat:latest,ANTHROPIC_API_KEY=anthropic-api-key:latest,MAINT_TRIGGER_TOKEN=maint-trigger-token:latest" \
            --quiet

      - name: Verify runtime service account
        run: |
          DEPLOYED_SA=$(gcloud run services describe "${{ env.SERVICE_NAME }}" \
            --region="${{ vars.GCP_REGION }}" \
            --project="${{ vars.GCP_PROJECT_ID }}" \
            --format='value(spec.template.spec.serviceAccountName)')
          if [ "$DEPLOYED_SA" != "${{ vars.RUNTIME_SA_EMAIL }}" ]; then
            echo "::error::Service is running as '$DEPLOYED_SA', expected '${{ vars.RUNTIME_SA_EMAIL }}'"
            exit 1
          fi
          echo "✓ Runtime SA verified: $DEPLOYED_SA"

      - name: Report deploy
        run: |
          URL=$(gcloud run services describe "${{ env.SERVICE_NAME }}" \
            --region="${{ vars.GCP_REGION }}" \
            --project="${{ vars.GCP_PROJECT_ID }}" \
            --format='value(status.url)')
          {
            echo "### ✅ Deployed"
            echo ""
            echo "- **Image**: \`${{ steps.image.outputs.uri }}\`"
            echo "- **Service URL**: $URL"
            echo "- **Commit**: \`${{ steps.image.outputs.sha }}\`"
          } >> "$GITHUB_STEP_SUMMARY"
